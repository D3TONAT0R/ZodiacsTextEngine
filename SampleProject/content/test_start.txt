@ENTER
+TEXT
You enter a mysterious cave.
<color=blue>The walls<clear> are covered in glowing runes.

You can see a faint light coming from deeper within the cave.

The runes say:
"What is your name, traveler?"
+FUNC prompt_player_name
+TEXT Welcome, <svar=player_name>!

IF player_name == test
	+TEXT You are a test user.
ELSE
	//Test if the name contains a E
	IF player_name ** e
		+TEXT You are special.
	ELSE
		+TEXT You are a regular traveler.
	ENDIF
ENDIF

+WAIT
+EXIT